# معماری فنی پروژه حامی نیو

## پشته تکنولوژی (Tech Stack)
- **فریم‌ورک اصلی**: Flutter (Dart) برای خروجی اندروید (APK/AAB) و ویندوز (EXE).
- **مدیریت وضعیت (State Management)**: Riverpod (به دلیل قابلیت تست‌پذیری بالا و جداسازی منطق از UI).
- **ذخیره‌سازی محلی**: `shared_preferences` یا `hive` برای تنظیمات سبک کاربر.
- **زیرساخت گت‌وی**: Cloudflare Workers + D1 (برای نهان/زئوس/نوا) و KV (برای BPB).

## ساختار پوشه‌بندی پروژه (Flutter)
```text
lib/
├── core/               # هسته اصلی (ثابت‌ها، تم، خطاها)
├── features/           # ماژول‌های ویژگی‌ها
│   ├── onboarding/     # راه‌اندازی اولیه و دریافت توکن
│   ├── dashboard/      # داشبورد اصلی و مدیریت پنل‌ها
│   └── settings/       # تنظیمات اپلیکیشن
├── shared/             # ویجت‌ها و توابع مشترک
└── main.dart           # نقطه ورود برنامه
assets/
├── panels/             # فایل‌های لوکال هر پنل (HTML/JS/CSS)
│   ├── nahan/
│   ├── bpb/
│   ├── zeus/
│   └── nova/
└── images/             # آیکون‌ها و تصاویر
