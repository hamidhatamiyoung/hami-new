# چشم‌انداز پروژه حامی نیو (Hami New)

## مأموریت
ساخت یک اپلیکیشن مدیریت گت‌وی (Gateway) امن، سبک و چندپنله (Multi-Panel) که بر پایه Cloudflare Workers اجرا می‌شود و تجربه‌ای یکپارچه برای کاربران نهایی فراهم می‌کند.

## اهداف کلان
1. **امنیت در اولویت اول**: حذف هرگونه وابستگی به دانلود زنده (Live Fetch) از منابع خارجی و لوکال کردن تمام دارایی‌ها (Assets).
2. **مدیریت هویت متمرکز**: استفاده از یک سیستم توکن یونیورسال (Universal Token) برای مدیریت دسترسی‌های کلودفلر با کمترین سطح امتیاز لازم (Principle of Least Privilege).
3. **پشتیبانی از چند پنل**: ادغام روان و ماژولار پنل‌های نهان (Nahan)، بی‌پی‌بی (BPB)، زئوس (Zeus) و نوا (Nova).
4. **مستندسازی زنده**: هر تغییر در کد باید با یک تصمیم ثبت‌شده در `DECISION_LOG.md` همراه باشد.

## ارزش‌های اصلی
- **شفافیت**: کد و تصمیمات برای هر ناظری قابل درک است.
- **تکرارپذیری**: فرآیند بیلد و دیپلوی باید بدون خطا و قابل بازتولید باشد.
- **استقلال**: عدم وابستگی به سرویس‌های شخص ثالث غیرقابل اعتماد (مثل ربات‌های تلگرام برای مدیریت توکن).
