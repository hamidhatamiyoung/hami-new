# سیاست‌های امنیتی (Security Policy)

این قوانین **غیرقابل مذاکره** هستند و هر تغییر در کد باید با آن‌ها سازگار باشد.

## قوانین طلایی
1. **ممنوعیت فچ زنده (No Live Fetch)**: هیچ فایل HTML، JS یا CSSای نباید در زمان اجرا از GitHub، CDN یا هر منبع خارجی دیگری دانلود شود. تمام فایل‌های پنل‌ها باید در پوشه `assets/` باندل شوند.
2. **مسیر مستقیم توکن**: کاربر برای ساخت توکن **فقط و فقط** باید به داشبورد رسمی کلودفلر (`dash.cloudflare.com`) هدایت شود. استفاده از ربات‌های تلگرام یا سایت‌های واسطه برای دریافت توکن اکیداً ممنوع است.
3. **اصل حداقل دسترسی (Least Privilege)**: توکن تولیدشده فقط باید دسترسی‌های ضروری (`workers_scripts_write`, `d1_write`, `workers_kv_storage_write`, `pages_write`, `user_read`) را داشته باشد.
4. **مدیریت ریسک پنل‌های شخص ثالث**: برای پنل‌هایی با لایسنس محدود (مثل زئوس)، باید یک دیالوگ سلب مسئولیت (Disclaimer) قبل از شروع فرآیند دیپلوی نمایش داده شود.
5. **عدم ثبت اطلاعات حساس**: توکن‌ها، آدرس‌های IP یا کلیدهای API هرگز نباید در لاگ‌های کنسول یا فایل‌های متنی ساده ذخیره شوند.
